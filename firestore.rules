rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for personal birthday card project
    match /cards/{cardId} {
      allow read: if true;
      allow write: if true;
    }
  }
}
